name: arrow_shared
description: Shared production configuration and BR helpers for Arrow Flutter apps.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.5.1
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
